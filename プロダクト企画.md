# Cat Terminal

## コンセプト

ねこが処理してくれるTerminal

コマンドを打つと、裏側でねこが動いている。故にコマンドが正しく動く保証はない。出力はもちろんねこ語。

`cat`コマンドを打つと…？

## プロダクトの方向性

### やらないこと

* Linux完全再現
* 本物のシェル
* 全コマンド対応
* 実用ツール化

### やること

* Terminal風UI
* 猫のリアクション
* 猫によるコマンド実行演出
* ちょっとしたゲーム性

---

## 基本フロー

ユーザー入力

```bash
cp a.txt b.txt

(=^･ω･^=) text... 

Success
```

または

```text
ERROR: ねこはファイルを落としてしまいました（すみません、ファイルは復元できません）。
```

※エラー文のみ、人語とする
※MVPでは、ねこを顔文字で表現する。可能ならドット絵、アニメーションなど機能拡充を行う。

---

## コマンド

### 必ず成功するコマンド

最低限、動くコマンドを用意する

- exit
- help
- コマンドが見つからなかった場合

#### exit

Terminalを終了するコマンド。
※任意のキー操作で再起動する。

#### help

実行可能なコマンドリスト。ただし説明は最小限とする。

```bash
$ help

ls
cat
...
```

#### コマンドが見つからなかった場合

存在しないコマンドを入力した場合

```bash
$ hogehoge
シャー！
Error: コマンドは存在しません。ねこ様はお怒りです。
```

### Linux風コマンド

MVPでは、Linuxコマンドを数個実装する。

- cat
- ls
- mkdir
- cp
- mv
- rm
- cd

#### cat

ネタコマンド。

```bash
$ cat a.txt
₍˄. .˄₎
```

※できればドット絵など。MVP期でも一番重要なコマンド。

#### ls

- ファイル一覧を表示する。
- エラーパターン
  - ランダムで失敗する

#### mkdir

- ディレクトリを作成する
- エラーパターン
  - ランダムで失敗する

#### cp

- ファイルをコピーする
- エラーパターン
  - ランダムで失敗する
  - 運ぶ途中に落としてしまう：`ERROR: ねこはファイルを落としてしまいました（すみません、ファイルは復元できません）。`

#### mv

- ファイルを移動する
- エラーパターン
  - ランダムで失敗する
  - 運ぶ途中に落としてしまう：`ERROR: ねこはファイルを落としてしまいました（すみません、ファイルは復元できません）。`

#### rm

- ファイルを削除する
- エラーパターン
  - ランダムで失敗する
  - ファイルを気に入って削除しない：`ERROR: ねこはこのファイルがお気に入りです。削除できません。`

#### cd

- ディレクトリを移動する
- エラーパターン
  - ランダムで失敗する

### 独自コマンド

- meow

#### meow

鳴く。

```bash
$ meow
にゃー
```

## エラー設計

### エラーメッセージは人語

ねこの反応だけだと意味不明になるため。メッセージは日本語とする

例

```text
ERROR: ねこはお昼寝中です。実行に失敗しました。
```

### 共通エラー

ほとんどのコマンドは、一定確率で失敗する。コマンド毎の特殊要件がない場合、エラーメッセージは以下の中からランダム表示とする。

```text
ERROR: ねこはお昼寝中です。実行に失敗しました。
ERROR: ねこは興味が無さそうにこちらを見ています。実行に失敗しました。
```

## 実装方針

OSやシェル実装ではなく、Terminal風UIを実装する。
HTML+JavaScriptで、静的ページとする（GitHub Pagesなどでホスティング可能な状態とする）。

### UI

画面の大部分はTerminalとし、最低限の情報はユーザーにも表示する。

- プロダクト名
- ファイル一覧（エクスプローラー）：ねこに関係なく、現在のファイル一覧が見える
