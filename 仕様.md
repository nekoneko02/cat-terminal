## Cat Terminal 外部仕様書

### 1. 画面レイアウト

```
┌─────────────────────────────────────────────────┐
│  🐱 Cat Terminal                                 │
├──────────────────────────┬──────────────────────┤
│                          │ FILES                │
│  Terminal エリア          │ ──────────────────── │
│  （メイン）               │ 📁 home/             │
│                          │   📄 readme.txt      │
│                          │   📁 docs/           │
│                          │                      │
│  $ _                     │                      │
└──────────────────────────┴──────────────────────┘
```

- ターミナルエリアとファイルエクスプローラーを横並び
- プロンプト形式：`neko@cat-terminal:~$ `（cdで変化）
- スクロール可能なターミナル出力履歴

---

### 2. 仮想ファイルシステム

- **メモリ上のみ**（ページリロードでリセット）
- 初期状態（仮定）：

```
/
└── home/
    ├── readme.txt
    ├── notes.txt
    └── docs/
        └── manual.txt
```

- ファイル・ディレクトリはオブジェクトとして管理（実ファイル内容は空 or ダミーテキスト）

---

### 3. コマンド仕様

#### 必ず成功するコマンド

| コマンド               | 動作                                       |
| ---------------------- | ------------------------------------------ |
| `help`                 | 対応コマンド一覧を表示（説明は最小限）     |
| `exit`                 | Terminal終了画面へ遷移。任意のキーで再起動 |
| （存在しないコマンド） | シャー！ + エラーメッセージ                |

#### Linux風コマンド（ランダム失敗あり）

| コマンド | 成功時の動作 | 失敗時の特殊メッセージ |
| --- | --- | --- |
| `ls [dir]`       | ファイル・ディレクトリ一覧をねこ語で表示 | - |
| `mkdir <name>`   | ディレクトリ作成 | - |
| `cp <src> <dst>` | ファイルコピー | 「落としてしまいました」 |
| `mv <src> <dst>` | ファイル移動 | 「落としてしまいました」 |
| `rm <file>`      | ファイル削除 | 「お気に入りなので削除できません」 |
| `cd <dir>`       | ディレクトリ移動（プロンプト更新）| - |
| `cat <file>`     | ネタ：ねこが出てくるだけ（ファイル内容は表示しない） | - |

**ランダム失敗確率**：各コマンドで **30%** の確率で失敗
特殊メッセージの有無にかかわらず、共通エラーメッセージを表示する。

#### 独自コマンド

| コマンド | 動作                         |
| -------- | ---------------------------- |
| `meow`   | 「にゃー」と出力（失敗なし） |

---

### 4. 出力仕様

#### 成功時

```
$ cp a.txt b.txt
(=^･ω･^=) ♪...
（ねこが処理中の演出）

にゃー（完了のねこ語）
```

- ねこの顔文字 + 処理中アニメーション（テキストのみ）
- 完了のねこ語はコマンド・結果によって変化

#### 失敗時

```
$ rm readme.txt
(=ΦェΦ=) !!

ERROR: ねこはこのファイルがお気に入りです。削除できません。
```

- エラー文は**日本語**（人語）

#### 共通エラー（ランダム）

```
ERROR: ねこはお昼寝中です。実行に失敗しました。
ERROR: ねこは興味が無さそうにこちらを見ています。実行に失敗しました。
```

---

### 5. その他

`ls` の出力形式： ディレクトリは色分けや`/`付与
`cat` の成功出力：AA（アスキーアート）
処理中アニメーション：テキストのみ（ドット増えるなど）
ファイルエクスプローラーの同期：コマンド成功時にリアルタイム更新する
モバイル対応：MVP では**PC専用**
